import React from 'react';

const ListagemHeader = (props) => {

    return (
        <div className="d-flex align-items-baseline" style={{marginTop: "-3vh"}}>
            <button className="btn btn-primary">Novo</button>
        </div>
    );
}

export default ListagemHeader;